# Sola 桌宠 · 稳定性修复版

已增加语音与 Codex 联动：点击角色下方“语音 / 对话”，或按 **Ctrl + Alt + V** 开关麦克风。可打字或连续语音交流、打断朗读、询问 Codex 进度。说“让 Codex …”可在面板底部指定的目录启动任务。“切换设备”可选择实际麦克风。完整使用与回档方法见 [README](README.md)。

已安装 Mate Engine 公开版 X3.3.0-HOTFIX-1。模型为你提供的 `D:\Blender\sola.blend` 的转换副本，原文件未修改。

## 启动和操作

- 双击桌面的 **Sola 桌宠**，或本文件夹的同名快捷方式。
- **F1** 打开圆形菜单；菜单打开时再次按 F1 可关闭。
- 按住角色拖动位置。鼠标停在角色身体上，滚轮向上放大、向下缩小；每格约 8%，不要求先点选窗口。
- 点击角色下方的 **显示设置**：可用滑块或 **− / +** 调整大小，用 **菜单字号** 调整完整菜单大小。设置自动保存。
- **完整设置** 打开原版功能菜单；**默认大小** 恢复模型显示的推荐大小。新增面板右上角 **×** 或面板获得焦点时按 **Esc** 可以收起，关闭状态会保存。底部的 **显示设置** 入口固定位置，不跟随舞蹈动作晃动。
- 默认以约 900 × 640 的小区域渲染。打开显示面板或完整菜单时自动展开到 1536 × 1024，收起后自动缩回；角色像素大小保持不变。模型变大时会自动留出更多空间，避免裁切。菜单缩放保持 115%，主要文字至少 20 个设计像素。
- **B** 切换大屏模式，也可使用菜单入口。
- 已选择简体中文，默认 **30 FPS** 以降低桌面常驻负担；画质和帧率仍可在完整设置中调整。未开启开机自启。
- 已开启窗口停靠。拖动角色到窗口上沿并停留后松开；是否能停靠取决于目标窗口类型。
- 音乐跟随默认识别 **QQMusic**。QQ 音乐播放并达到音量阈值时会触发跳舞；其他播放器可在舞蹈设置中添加。
- 使用聊天菜单进行本地中文对话。首次打开需要初始化模型，可能稍等数十秒；模型已下载，无需再次下载，也无需 API Key。
- 闹钟和计时器可从菜单设置。测试用计时器已移除。

## 模型与观感

推荐模型：`models\Sola-Soft-NPR-v1.1.vrm`。

可编辑的转换副本：`models\Sola-Soft-NPR-v1.1.blend`。

保留原始色彩和贴图，使用 MToon 柔和渐变阴影，降低镜面高光，关闭泛光和环境遮蔽，避免脸部脏阴影。已补充左右眼骨骼、眨眼、五种元音口型和基础情绪表情。鼠标眼神跟随采用较低强度。

桌面透明方式使用 ColorKey：只抠除固定背景色，窗口不透明度保持 100%。已禁用后处理中的抖色步骤；否则背景颜色会被随机改变，在桌面上形成雪花噪点。其他后处理入口保留。此模式不保留连续的半透明效果，边缘可能比 Alpha 模式略硬；原模型主体为不透明材质。

原模型的眼睛和嘴巴主要画在贴图上，本版通过局部网格变形及口腔补面适配；近距离仍能看出原模型的低多边形轮廓。头发保持原有骨骼方式，没有新增独立发丝物理。这是可继续打磨的第一版。

## 存储位置

实际目录：`D:\桌宠\MateEngine-Sola`。

`D:\MateEngine-Sola` 是同一目录的英文路径入口，不是第二份副本。

| 内容 | 位置 |
|---|---|
| 程序及本地聊天模型 | `app` |
| Sola 模型 | `models` |
| 设置、模型库、聊天历史及缓存 | `userdata` |
| 新增显示偏好 | `userdata\display.json` |
| 原版安装包 | `downloads` |
| 转换、修复和验证脚本 | `work` |

程序默认写入的 `C:\Users\Zamateur\AppData\LocalLow\Shinymoon\MateEngineX` 已通过目录联接指向这里的 `userdata`。启动器额外指定 `--datadir D:\MateEngine-Sola\userdata`，让核心设置直接从 D 盘加载和保存，并把子进程临时目录设到 D 盘。

## 功能范围和验证

完整公开版程序和原版资源已安装，Steam 授权状态保持原样。Steam 创意工坊、专属内容，以及本版本界面标为需要 Steam 的功能，需要另行拥有 Steam 版。

已实际验证：VRM 导入和人形骨架、中文本地聊天及聊天界面、待机和跳舞动画、拖拽动画、表情/口型、粒子效果、Q 版切换、计时器启动及到期触发大屏、设置保存和重启加载。

已启用窗口停靠并检查相关组件和坐姿；未逐一实测所有第三方窗口的吸附行为。鼠标实际拖放、所有触摸区域、所有屏幕布局也没有逐项人工验收。

自定义舞蹈、MMD 和模组入口保留，需要相应的第三方素材才能使用；没有为验证而随机安装第三方模组。Discord 状态广播保持关闭，可以按需自行开启。

本地聊天使用公开版本配置的 Llama 3.2 3B 量化模型。它适合轻量对话，不提供语音朗读，也不能替你操作桌面。人格提示词可在程序设置中编辑，对应 `userdata\ZomeAI_prompt.txt`。

## 本地兼容修复

保留原版 DLL 备份：`work\Assembly-CSharp.original.dll`。

本地修复包括：等待语言资源初始化后再设置语言；自定义模型加载/退出时的空对象检查；为空的 AI 对话角色补上 `user` 和 `assistant`。未修改 Steam 授权逻辑。

显示优化包括：背景色抠除、可读的默认菜单尺寸、中文显示面板、模型缩放范围 0.35～2.4（原上限 1.28）、原生滚轮接收和延迟保存、菜单边界适配。滚轮只在本程序的角色区域处理；菜单区域保留正常滚动。骨架状态检查改用有效人形骨架，修复 Mixamo 骨骼命名被误报为 Failure 的问题。

稳定性修复：新增面板独立创建，不再复制整套原版菜单；缓存界面对象，去掉每帧全局扫描；修正透明拖拽层抢占按钮点击的优先级；关闭面板后自动缩小渲染区域；仅更改模型缩放，不再因滚轮操作重置舞蹈和交互状态。原版后处理 DLL 备份在 `work\Unity.Postprocessing.Runtime.original.dll`。

修复源码 `work\SolaSupport.cs`、`work\SolaDisplay.cs`，构建脚本 `work\build_support.ps1`；模型转换脚本 `work\build_sola_vrm.py`、`work\sola_expressions.py`。本次优化前的可恢复备份在 `work\before-usability-20260915-112519`。

最新检查记录为 `work\stability-verification.json`；发生雪花问题的上一版备份在 `work\before-stability-20260915`，用于排查，不建议恢复为日常版本。

开发验证接口仅在使用 `--sola-diagnostics` 启动时工作，不监听网络；桌面快捷方式的正常启动不启用它。

更新上游程序可能覆盖本地兼容修复，请先保留此文件夹和 `userdata`。

来源：[Mate Engine](https://github.com/shinyflvre/Mate-Engine)、[VRM Add-on for Blender](https://github.com/saturday06/VRM-Addon-for-Blender)、[本地聊天模型](https://huggingface.co/hugging-quants/Llama-3.2-3B-Instruct-Q4_K_M-GGUF)。模型与上游资源各自保留原有权利和许可。
